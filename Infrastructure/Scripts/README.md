# Scripts

Shell scripts used by the workspace. The `prima/` subfolder contains state management scripts (backup, validation) used by session and project skills.

You can add your own scripts here as you build automations.

**Why this file exists:** scaffolding marker. Delete when you add your own scripts.
