# Infrastructure

Operational scripts, utilities, and configuration that support the workspace itself (not individual projects or clients).

The `Scripts/` subfolder contains shell scripts Cowork uses for state management (backup, validation).

**Why this file exists:** scaffolding marker. If you add infrastructure of your own, delete this README.
