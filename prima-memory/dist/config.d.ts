/**
 * Central path configuration for PRIMA Memory.
 *
 * All paths are resolved at startup based on the workspace that launched
 * the server. No hardcoded user-specific paths — works for any user
 * on any machine.
 */
export declare const WORKSPACE_ROOT: string;
export declare const PROJECTS_DIR: string;
export declare const WORKSPACE_PROJECT_DIR: string;
export declare const DB_DIR: string;
export declare const DB_PATH: string;
export declare const EXTRACTS_DIR: string;
